import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Ficha Ordem Mobile',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
